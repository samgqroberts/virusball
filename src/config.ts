export default {
  CIRCLE_RADIUS: 0.1, // in clipspace, i think
  CIRCLE_STARTING_X: -0.2,
  CIRCLE_STARTING_Y: 0,
  TICK_VELOCITY: 0.01, // how much to move player per tick while player has a key pressed

  // for debugging
  LOG_FPS: false,
  ONLY_DRAW_ONCE: false
};
